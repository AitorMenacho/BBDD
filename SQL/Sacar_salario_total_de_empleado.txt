CREATE OR REPLACE FUNCTION SALARIO_TOTAL
(V_EMPNO EMP.EMPNO%TYPE)
RETURN NUMBER
AS

   V_SUMA NUMBER(10) := 0;

BEGIN

    SELECT SUM(EMP.SAL + EMP.COMM) INTO V_SUMA
    FROM EMP
    WHERE EMPNO = V_EMPNO;
    
    RETURN V_SUMA;

END;


--Ejecución

DECLARE

    V_EMPNO EMP.EMPNO%TYPE := &CODIGO;
    v_suma number(10);

BEGIN

    v_suma := SALARIO_TOTAL(V_EMPNO);
    DBMS_OUTPUT.put_line('LA SUMA DE COBRO ES DE ' || v_suma);

END;