create or replace FUNCTION NOMBRE_DEPARTAMENTO
(V_DEPTNO DEPT.DEPTNO%TYPE)
RETURN VARCHAR2
AS

   V_DEPARTAMENTO varchar2(20) := 0;

BEGIN

    SELECT DNAME
    INTO V_DEPARTAMENTO
    FROM DEPT
    WHERE DEPTNO = V_DEPTNO;
    
    RETURN V_DEPARTAMENTO;

END;